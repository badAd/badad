# Launch To-Do
- Your message does not contain a List-Unsubscribe header
  - The List-Unsubscribe header is required if you send mass emails, it enables the user to easily unsubscribe from your mailing list.
  - https://stackoverflow.com/questions/4365850/list-unsubscribe-in-e-mail-header-how-to
  - Create an email unsubscribe auto-processor using sieve

# Roadmap To-Do
- AJAX for partner.php & partner_dev.php
  - The code is written, but
    - AJAX doesn't work
    - partner_dev.php AJAX handler files don't have the full <div> to echo in response
    - file are locally prepped in webdir/ajax until ready

| **Error code** :

```php

//In case you want to show errors
ini_set('display_errors', '1');
ini_set('display_startup_errors', '1');
error_reporting(E_ALL);

```
